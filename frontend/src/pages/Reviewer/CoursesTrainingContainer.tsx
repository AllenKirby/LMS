import { Outlet } from "react-router-dom"

const CoursesTrainingContainer = () => {
  return (
    <section className="w-full h-full">
        <Outlet/>
    </section>
  )
}

export default CoursesTrainingContainer